# PDN recipe template
puts "## PDN: style=$style via_stack=$via_stack decap_ratio=$decap_ratio" #这句话是输出PDN配方的注释，包含风格、通孔堆栈和去耦电容比率
# foreach strap: layer/pitch/width
# generate rings/mesh; add vias; place decaps; export reports/ir_em.rpt
